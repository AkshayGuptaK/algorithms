# Given k sorted arrays of length l, design a deterministic algorithm (i.e. an algorithm that
# uses no randomness) to find the median element of all the n = kl elements. Your algorithm
# should run asymptotically faster than O(n).


def main(arrays):
    return None
